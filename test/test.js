import { Something } from '../some/place';

const foo = require('bar');

function foo(x) {
    const bar = x === 31;
    return x * 2;
}

const bazzzz = true == foo ? 42 : 24;

const foo = ["bar", [234], ('baz'), 2.34, `hmm`];
